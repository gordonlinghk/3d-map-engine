import * as THREE from 'three';
import type { MapWorld } from '@map-engine/core';
/** Star field on a high dome — visible only at night. */
export declare function buildStars(world: MapWorld): THREE.Points;
